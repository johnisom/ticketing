<h2><a href="<%= root_path %>">Ticketing</a></h2>

<nav>
  <ul>
    <li><a href="<%= projects_path %>">Projects</a></li>
    <li><a href="<%= tickets_path %>">Tickets</a></li>
    <li><a href="<%= tags_path %>">Tags</a></li>
  </ul>
</nav>

<span>
  <% if logged_in? %>
    <%= current_user.name %>
    <%= link_to 'Log Out', logout_path %>
  <% else %>
    <%= link_to 'Sign Up', signup_path %>
    or
    <%= link_to 'Log In', login_path %>
  <% end %>
</span>
