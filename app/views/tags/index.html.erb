<h1>Tags</h1>

<% if @tags.size.zero? %>
  <em>
    There are no tags.
    <% if logged_in? %>
      Want some?
      <%= link_to 'Create one!', new_tag_path %>
    <% end %>
  </em>
<% else %>
  <%= render 'tags' %>
<% end%>

<br>

<%= link_to('New Tag', new_tag_path) if logged_in? %>
